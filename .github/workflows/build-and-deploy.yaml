name: Deploy React + PHP API to Namecheap

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout
      - name: Checkout Repo
        uses: actions/checkout@v3

      # -------------------------------------------------
      # 2. Build React App
      # -------------------------------------------------
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install React dependencies
        working-directory: ./app
        run: npm install

      - name: Build React app
        working-directory: ./app
        run: npm run build

      # -------------------------------------------------
      # 3. Deploy React Build to / (public_html)
      # -------------------------------------------------
      - name: Deploy React App
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          port: 21
          local-dir: ./app/dist/
          server-dir: /
          # 🔥 Prevent deletion of server-side files like .htaccess
          dangerous-clean-slate: false

      # -------------------------------------------------
      # 4. Prepare API
      # -------------------------------------------------
      # - name: Create PHP API .env file
      #   run: |
      #     echo "SMTP_USERNAME=${{ secrets.SMTP_USERNAME }}" > ./api/.env
      #     echo "SMTP_PASSWORD=${{ secrets.SMTP_PASSWORD }}" >> ./api/.env

      # -------------------------------------------------
      # 5. Deploy PHP API
      # -------------------------------------------------
      # - name: Deploy PHP API
      #   uses: SamKirkland/FTP-Deploy-Action@4.3.0
      #   with:
      #     server: ${{ secrets.FTP_HOST }}
      #     username: ${{ secrets.FTP_USERNAME }}
      #     password: ${{ secrets.FTP_PASSWORD }}
      #     protocol: ftps
      #     port: 21
      #     local-dir: ./api/
      #     server-dir: /api/
      #     # 🔥 Prevent wiping the folder
      #     dangerous-clean-slate: false
      #     # 🔥 Ignore vendor so they don’t get stuck or corrupted
      #     exclude: |
      #       **/vendor/**
      #       **/*.log
